model orders {
  fields {
    user_id() int
    user_name() string
    order_count() int
  }

  gens {
    func user_id() {
      return self.datagen.users().id(iter)
    }

    func user_name() {
      return self.datagen.users().name(iter)
    }

    func order_count() {
      return IntBetween(0, 10)
    }
  }
}