func (e *__datagen_{{.FullyQualifiedModelName}}) __dgi_ToJSON() string {
    data, err := json.Marshal(map[string]interface{}{
        {{- range .Fields}}
        "{{.Name}}": e.{{.Name}},
        {{- end}}
    })
    if err != nil {
        fmt.Println(err)
        return ""
    }
    return string(data)
}